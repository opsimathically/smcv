# Portable archive compatibility fixtures

`v1-minimal.smcvault` is a synthetic, non-production logical-vault fixture
generated by the `smcv-app` `generate_v1_fixture` example. Its synthetic
recovery key is recorded here solely
so automated compatibility tests can authenticate it. Never reuse it for a
real archive.

Synthetic fixture key:
`smcvbrk_v1.M6_qs6hHm50zrqXxU3vlWWCdK8FWcnIhAkiuqMnITp0.83d973d9`

The binary fixture is immutable compatibility input. Regenerating it is a
format-change action requiring an explicit compatibility review.
